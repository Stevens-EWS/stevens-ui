# Stevens UI

## Development

### Install dependencies
#### `npm install`

### Build the library
#### `npm run rollup`

### Run Storybook locally
#### `npm run storybook`

### Build Storybook
#### `npm run build-storybook`
